# CLAUDE.md — AutoAgent Test

Guidance for Claude Code when working in this repo. Keep this file updated whenever plan status, architecture, or project conventions change.

## Project

AutoAgent Test — backend service for batch testing conversational AI products via OpenAI-compatible APIs. Python 3.10+, FastAPI, async SQLAlchemy (SQLite + aiosqlite), JWT auth, per-batch JSONL result files, YAML profile registry, webhook callbacks.

## Development status

Source of truth: `docs/superpowers/plans/` and `docs/superpowers/specs/`. Update this section when a plan completes or a new one starts.

- **Plan 1 — Backend MVP:** ✅ complete (tag `backend-mvp-v0.1.0`, 2026-04-22). All 25 tasks done. 66 tests passing, ruff clean.
- **Plan 2 — React Web UI:** ✅ complete (tag `web-ui-v0.2.0`, 2026-04-22). Single-binary deploy (FastAPI serves built SPA from `src/autoagent/static/` with SPA fallback). 2s polling via TanStack Query for batch progress (WebSocket deferred to Plan 3). 68 backend tests + 8 frontend tests green; browser smoke (login → profiles → dry_run batch → download → config → logout) passed.
  Important runtime note: this repo uses a `src/` layout. In a git worktree, run uvicorn with `--app-dir src` (or equivalent `PYTHONPATH=src`) so the current checkout is imported instead of an older editable install from another checkout.
- **Plan 3 — Web GUI Executor (Playwright):** ✅ complete (tag `web-gui-executor-v0.3.0`, 2026-04-22). Playwright-backed `WebExecutor`, in-process event bus + SSE, screenshot APIs, web connectivity testing, `BatchDetail` SSE streaming, and `SampleDetail` screenshot/action-log UI are in the repo. Verification status: backend full suite `128 passed` when run outside the sandbox so Chromium can launch; backend fast suite `123 passed, 5 deselected`; frontend `pnpm test`, `pnpm lint`, `pnpm format:check`, and `pnpm build` all green; manual browser smoke passed end-to-end.
- **Plan 4 — Android Executor (uiautomator2 + OCR):** ✅ complete (tag `android-executor-v0.4.0`, 2026-04-27). Full feature set: device discovery, `/devices` API + IME management, `gui_android` scheduling, Android Profile Builder (rule/smart draft modes, Review flow, connectivity validation, new_session_action guided tap-sequence authoring), OCR long-response stitching, `pixel_stable`/`ui_tree_stable`, opt-in LLM response extraction, SampleDetail dual-view. Backend fast suite 293 passed; frontend 21 passed; lint/build green.
- **Post-Plan 4 fixes (2026-04-27):** `WebExecutor` session persistence — browser now reused across samples within a batch (`new_session=false` skips re-navigation; `new_session=true` runs `new_session_action` or reloads in-place; error invalidates session for clean retry). `WebBrowserConfig` gains `channel` field (default `"chromium"`, use `"chrome"` for system Chrome with user_data_dir). Profile registry now skips invalid YAMLs with a warning instead of crashing `/api/v1/profiles`. Backend fast suite 308 passed.
- **Plan 5 — Polish (packaging, backups, Docker, security hardening):** partially done. The security-hardening backlog (SecretStr, JWT hardening, login timing) is closed — see below. Packaging/backups/Docker still not started.
- **Plan 6 — Agent Executor (agent_pc + agent_android):** complete in repo (2026-05-06). Added `agent_pc` and `agent_android` modes with shared `agent_core` loop, screenshot-based response extraction, executor registration, and Android device-pool scheduling support. Verification: fast backend suite `363 passed, 1 skipped, 18 deselected`; targeted new-mode suite `19 passed`. Source of truth: `docs/superpowers/plans/2026-05-06-agent-executor.md`.
- **Agent runtime unification (2026-05-06, ongoing hardening):** `agent_core` was split into focused modules (`parser.py`, `result.py`, `runtime.py`, `handlers/`, `devices/`) derived from `apa_llm`/`Open-AutoGLM`, behind the same `AgentPcExecutor`/`AgentAndroidExecutor` boundary. Notable behavior: text-only conversation context is preserved across steps; `element`/`start`/`end` coordinates use a 0-1000 relative contract converted to pixels via `screen_width`/`screen_height`; PC `Type` uses clipboard-copy + paste shortcut instead of `pyautogui.typewrite()` for reliable non-ASCII input; a multimodal screenshot-based observer gates `finish(...)` after send-like actions and verifies `Type` actually landed in the input box before allowing `Press(enter)` to proceed (guards against duplicate/premature sends). Source of truth: `docs/superpowers/plans/2026-05-06-agent-runtime-unification.md`.
- **Install script (2026-05-07):** `install.sh` (OS detection + system deps: Python 3.11, Node.js, adb, uv, pnpm via Homebrew/apt+deadsnakes/dnf) calls `scripts/setup.py` (Python-level: `uv sync`, `pnpm build`, `playwright install chromium`, interactive `.env` generation).
- **OpenAI chat completions compat (2026-05-10):** `POST /v1/chat/completions` (non-streaming) lets OpenAI SDK clients drive the existing sync-test pipeline. `model` maps to `target_profile`; only the last `user` message is used; `stream=true` unsupported; AutoAgent extension fields (`new_session`, `timeout_sec`, `retry`, `dry_run`) go in `extra_body`. Shared execution logic lives in `services/sync_tests.py`, used by both `/api/v1/tests/sync` and `/v1/chat/completions`.
- **Static API key auth (2026-05-10):** optional `STATIC_API_KEY` env var provides one long-lived Bearer key that works across `/api/v1/*` and `/v1/chat/completions` alongside JWT, via a shared bearer-resolution layer in `auth/bearer.py` (`hmac.compare_digest`; authenticates as `admin`).
- **Device screen streaming (2026-05-15):** Devices page can open a modal with live H264-over-WebSocket screen streaming (`adb exec-out screenrecord`) plus tap/swipe/text/system-key interaction, via `api/device_stream.py` (backend) and `useDeviceStream` + `DeviceStreamModal` (frontend, WebCodecs `VideoDecoder`).
- **CI (2026-07-16, actually verified green 2026-07-22):** `.github/workflows/ci.yml` runs on every push/PR to `main` — backend job (`uv sync --extra dev` → `ruff check .` → fast pytest suite) and frontend job (`pnpm install --frozen-lockfile` → `pnpm lint` → `pnpm test` → `pnpm build`). Neither gates on `ruff format --check`/`pnpm format:check` — both have pre-existing formatting drift (69 backend files, 36 frontend files) that predates this workflow; fixing that is a separate mass-reformat task, not something CI should block on yet.
  It failed on every run from creation until 2026-07-22 — it had only ever been validated by spot-checking locally-touched files, never by watching an actual GitHub Actions run. Root causes, all now fixed: (1) `uv.lock` was gitignored, so `astral-sh/setup-uv`'s cache step failed before installing anything — `uv.lock` is now committed (this also silently blinded `system/updater.py`'s "did a pulled commit touch `pyproject.toml`/`uv.lock`" check, since an untracked file never shows up in `git diff`); (2) `ruff check .` had never actually been run unscoped against the whole repo — doing so surfaced 86 pre-existing errors (7 genuine and fixed, 23 F811 + 3 F401 that are false positives from a cross-file pytest-fixture-import pattern, 56 E501 that are the same pre-existing formatting drift as above), now clean via targeted `extend-per-file-ignores`; (3) `tests/unit/test_android_executor_unit.py` shelled out to a real `adb` binary via `ensure_screen_awake`, unmocked — worked by accident on any machine with adb installed (fails soft against a nonexistent device) but raises `FileNotFoundError` where adb isn't installed at all, i.e. GitHub's runner — now mocked like every other executor dependency in that file; (4) `web/src/pages/Profiles/Builder.test.tsx`'s heaviest flows run ~3x slower on GitHub's shared runners than locally, tipping different individual tests over vitest's default 5000ms timeout on different runs. First attempted as a file-scoped `vi.setConfig({ testTimeout: 20000 })` in a `beforeAll` — looked right and passed locally, but two further CI runs each still hit the 5000ms default on a different test, so that runtime mutation isn't reliably honored for already-scheduled tests. Fixed for real via `test.testTimeout: 20000` in `web/vite.config.ts` (read once at startup, no ordering ambiguity). Lesson: verify CI by watching an actual run (`gh run watch` / `gh run view --json conclusion`) — including re-verifying after each attempted fix — not by locally re-deriving what you assume it does, and not by treating "passed locally" as proof it'll pass on CI's slower/different runners.
- **DB indexes (2026-07-16):** `Batch.status`, `Sample.status`, `Sample.target_profile` are now indexed (alongside the pre-existing `Batch.created_at`) — `count_batches_by_status`/filtered `list_batches` were full-scanning `samples`/`batches` as history accumulates, especially now that archive retention keeps that history around longer. `storage/database.py::init_db()` has `CREATE INDEX IF NOT EXISTS` migrations for existing DBs (SQLAlchemy's `create_all()` only adds indexes for brand-new tables).
- **Batch list pagination cap (2026-07-16):** `GET /api/v1/batches` now rejects `limit>200` (422) — matches the UI's own largest `pageSizeOptions` choice, so a crafted/huge `limit` can't force a multi-thousand-row scan + per-row preview/profile aggregation.
- **Devices bulk operations + reachable enable/disable (2026-07-16):** the backend has had `POST /devices/{serial}/connect` and `/disconnect` (toggle `Device.enabled`, which gates `DevicePool` scheduling) since early on, but no frontend ever called them — enable/disable was completely unreachable from the UI. Added `useConnectDevice`/`useDisconnectDevice`, a per-row 启用/禁用 button, and a checkbox-selection toolbar (批量启用/批量禁用/批量删除) mirroring the Batches List bulk-ops pattern (`Promise.allSettled` over the existing single-device endpoints, no new bulk backend route).
- **Security hardening (2026-07-16, closes the Plan 5 backlog below):** `Settings.admin_password`/`jwt_secret`/`static_api_key` are now `pydantic.SecretStr` (callers use `.get_secret_value()`; `repr(settings)` no longer leaks them). JWTs now carry and verify `aud="autoagent-api"`/`iss="autoagent"` claims (`auth/jwt.py`) plus `leeway=30s` for clock skew — **this invalidates all previously-issued tokens**, so a deploy of this change logs everyone out (self-heals within `jwt_expires_hours`, 24h by default). `auth/bearer.py`'s `except Exception` around `decode_token` is narrowed to `jwt.PyJWTError` so a real bug there isn't silently reported as "invalid token". `api/auth.py`'s login endpoint now runs a real bcrypt check against a fixed dummy hash for unknown usernames instead of short-circuiting past `verify_password`, closing the username-enumeration timing side-channel. `default_verbose_logs` flipped `True` → `False` (verbose per-action screenshots are opt-in now, not a footgun default for disk usage at scale).
- **WebBuilder consistency fix (2026-07-20):** `web/src/pages/Profiles/WebBuilder.tsx` was the only page importing the static `message` API directly from `'antd'` instead of `App.useApp()`'s context-bound instance (the pattern every other page follows so messages consume the dynamic theme correctly). Switched to `const { message } = App.useApp()`.
- **Batch list N+1 fix (2026-07-21):** `GET /api/v1/batches`'s single-sample preview (`_single_sample_preview`) used to call `list_samples_for_batch` — a fresh DB session per call — once per `total==1` batch in the page, up to `limit` (200) extra queries per request. Added `storage/samples.py::list_samples_for_batches` (one `WHERE batch_id IN (...)` query) so `list_all` fetches all single-sample batches' rows in one round trip, mirroring the existing `batch_profiles_and_devices` aggregate-query pattern.
- **`useBatchStream` reconnect-storm fix (2026-07-22):** the SSE effect in `web/src/hooks/useBatchStream.ts` depended on `query.data`, but every SSE event calls `queryClient.setQueryData(...)`, which produces a new `data` reference — so the effect was tearing down and reopening the `/events` connection on *every single event*. The backend's `subscribe()` doesn't replay buffered events on a fresh subscription (`BatchEventBus.replay_since` exists but the SSE route never calls it), so events published in the abort/reconnect gap were silently dropped. Fixed by gating/depending on `query.isSuccess` instead (flips false→true once on initial load, stays stable across cache updates). Regression test added in `useBatchStream.test.ts` asserting exactly one `/events` fetch across a multi-event stream.
- **Upload size limit (2026-07-22):** `POST /api/v1/batches/upload` called `file.read()`/`.decode()` with no size check — a large upload (accidental or not) would get fully materialized in process memory, with no reverse-proxy body-size limit in front of this single-process service to catch it first. Now rejects uploads over 50MB (`_MAX_UPLOAD_BYTES` in `api/batches.py`) using `UploadFile.size`, which Starlette's multipart parser has already populated by the time the handler runs.
- **Login lockout (2026-07-22):** `POST /api/v1/auth/login` had no brute-force protection — notable because `CLAUDE.md` documents a well-known default admin password (`admin123456`) for dev convenience, and bcrypt's per-attempt cost alone isn't a real deterrent. Added `auth/login_throttle.py`: an in-memory, per-username failure counter (5 failures → 15min lockout, `429`), bounded to 10k tracked usernames with oldest-eviction (same pattern as `media.py`'s thumbnail cache) so flooding distinct usernames can't grow it unboundedly. Tracking keys off the raw submitted username regardless of whether the account exists, so the lockout signal doesn't reopen the username-enumeration side channel the dummy-hash timing fix closed. Single-process/in-memory by design — resets on restart, doesn't share state across workers.
- **Data backup (2026-07-23, starts closing out Plan 5's backup item):** `data/` had no backup story at all — a disk failure or `rm -rf data/` was unrecoverable. Added `maintenance/backup.py`: a periodic job (mirrors `batch_retention.py`'s pattern) that zips the SQLite DB + `data/profiles/*.yaml` into `data/backups/<timestamp>.zip`. The DB is copied via sqlite3's online backup API (`Connection.backup`), not a raw file copy — this project runs SQLite in WAL mode, so a plain copy can miss committed data still sitting in the `-wal` file and produce a torn snapshot. Deliberately excludes results JSONL/logs/archived batches/browser_profiles/profile_builder artifacts — results are mostly a queryable subset of what's in the DB, and logs/archives already have their own retention+archive-before-delete story; backing them up again here would just duplicate that at real disk cost. Controlled by two new `DefaultsConfig` fields (`backup_retention_days`, default 14, 0 disables; `backup_interval_hours`, default 24) surfaced on the Config page alongside a "立即备份" manual-trigger button and a list of existing backups. `run_backup_loop()` wired into `main.py`'s lifespan background tasks. No restore UI by design — restoring is a rare, high-stakes admin action better done deliberately (unzip, stop the service, replace `data/db.sqlite`, restart) than exposed as a self-service button; see "Restoring from a backup" under Conventions.
- **DB migrations moved to Alembic (2026-07-23):** `storage/database.py::init_db()` used to hand-roll every schema change as a `PRAGMA table_info` + conditional `ALTER TABLE`/`CREATE INDEX IF NOT EXISTS` check, all re-run unconditionally on every single boot forever — including an unindexed full-table `LIKE` scan of `samples` (`_backfill_prompts_ensure_ascii`) for a data-fix that's been a no-op for any row written after that fix originally landed. Replaced with real Alembic migrations under `alembic/versions/` (baseline schema + the backfill as a one-time data migration). `init_db()` now branches three ways: a fresh DB (every pytest run, any new install) gets the schema via `Base.metadata.create_all()` directly — fast, no migration replay — then is stamped to head; a pre-existing DB with no `alembic_version` table gets stamped to *baseline* then upgraded normally, so the backfill still actually runs once for it; an already-managed DB just gets `upgrade head`. Verified against a real pre-Alembic DB (created via the old code path) that stamping doesn't error re-creating existing tables/columns and the backfill correctly fires exactly once. Full fast suite timing is unchanged (~182s) — the fresh-DB fast path avoids paying migration-replay overhead on the hundreds of per-test DB creations. Caught one real bug along the way: Alembic's generated `env.py` calls `logging.config.fileConfig()` by default, which disables every pre-existing logger not listed in `alembic.ini` — since `init_db()` now runs this on every boot, it was silently breaking the app's own logging (`utils/logging.py`) each time; removed that call (see Conventions).
- **Session-level device stickiness (2026-07-24):** multi-turn conversations built as a sequence of separate requests (`new_session=false` continuations via repeated `/api/v1/tests/sync` or `/v1/chat/completions` calls, as opposed to multiple `prompts` in one `Sample`) had no way to stay on the same physical device once a profile had more than one — each request independently picked "whichever device is free," so a continuation could silently land on a different device with no memory of the prior turns. Added opt-in `Sample.session_id` plus session-pin tracking in `devices/pool.py::DevicePool` and an explicit `POST /api/v1/devices/sessions/{session_id}/release` — see "Session-level device stickiness" under Conventions for the full mechanics. Also wired through `openai_compat/schemas.py` (`session_id` in `extra_body`). First cut made reservation a *soft* preference (fall back to using a reserved device rather than starve a new conversation when there was no alternative) — correct per-test but wrong for the actual requirement: a device mid-conversation must be fully off-limits to everyone else, full stop. Reworked to hard-exclude reserved devices unconditionally (candidates just wait, same as a locked device, when reservations leave nothing else available). Follow-up: added `DeviceReserved` (immediate 429-worthy failure naming the blocking `session_id`(s), instead of waiting, for `session_id`-bearing callers when every device is reserved) and `Sample.end_session` (release a conversation's device without sending a real turn). Also fixed a real pre-existing gap this surfaced: `DeviceBusy`/`DeviceDisabled` from `acquire()` had no handler anywhere in `batch_scheduler.py`, so it crashed the batch's background task uncaught instead of recording a failed sample — see "Session-level device stickiness" under Conventions.

- **Batch list status/mode filter fix (2026-07-27):** `GET /api/v1/batches` never accepted `status`/`mode` query params — the Batches page parsed both from the URL but only ever applied them via `Array.filter()` to whatever page `useBatches` had already fetched (default 20 rows, newest-first). If no row on that page matched, the table rendered empty even when matching batches existed elsewhere, and the pagination `total` used the unfiltered grand total instead of the filtered count. Added `status`/`mode` params to `list_batches()`/`GET /batches`, and a `mode` param (no `status` — it groups by status) to `count_batches_by_status()`/`GET /batches/stats`; frontend now sends both server-side instead of re-filtering fetched rows, and derives pagination `total` from `stats[statusFilter]` when a status filter is active.
- **OpenAI-compat integration doc + structured 429 error field (2026-07-22, undocumented until now):** `docs/openai_compat_integration.md` is a Chinese-language integration guide for external callers of `/v1/chat/completions` (auth, field mapping, multi-turn/`session_id` usage scoped to `android`/`agent_android`, the two-layer timeout model, error table). While writing it, found `/v1/chat/completions`'s 429 only exposed `blocking_session_ids` as an unparseable Python-list-repr substring inside `error.message`, unlike `/api/v1/tests/sync`'s clean JSON array — fixed via a new `OpenAIErrorResponse.x_autoagent`/`OpenAICompatError.extra` field (mirrors the success-response `x_autoagent` envelope) so both endpoints expose the same structured data.

- **Device session visibility + release UI (2026-07-27):** session-level device stickiness (`Sample.session_id`, see above) had no way to inspect or manage from outside the API — `POST /devices/sessions/{id}/release` existed but only worked if you already knew the `session_id`, and there was no way to see which sessions currently hold a device (e.g. to spot one stuck past when it should've been released). Added `DevicePool.list_sessions()` + `GET /api/v1/devices/sessions`, and a "多轮对话设备占用" panel on the Devices page (session_id / occupied device / remaining TTL / 释放 button) wired to the existing release endpoint. 释放 is Popconfirm-gated — releasing a still-in-progress conversation's device lets its next turn land on a different device, which silently breaks that conversation's context (same risk noted in `DevicePool.acquire`'s docstring). Follow-up same day: added a 一键释放全部 button in the panel header (also Popconfirm-gated) that fans out `Promise.allSettled` over the same release endpoint for every currently active session, mirroring this page's own 批量启用/批量禁用/批量删除 bulk-op pattern. Second follow-up: the panel started as an always-rendered Card above the main device table — full table chrome (header row + AntD's empty state) taking real vertical space for what's usually zero active sessions. Collapsed into a 会话占用 button (Badge shows the live count) in the PageHeader controls that opens the same table + 一键释放全部 in a Popover on demand, mirroring Batches List's 筛选 button; verified in a real browser (idle state is now one button next to 刷新, detail still full when opened).
- **画面 grid pagination + per-profile device screen viewer (2026-07-27):** each 画面 card opens its own websocket + H264 decoder (`DeviceStreamCard`); with 30 online devices in production, switching to 画面 opened 30 concurrent `adb screenrecord` processes on the backend at once, reliably timing out. Extracted `components/DeviceScreenGrid.tsx` — paginated (8/12/16/24 per page; pagination UI only shows above 8 devices), only mounts (and thus streams) the current page's cards — and swapped it into the Devices page's 画面 view in place of the old unpaginated `Row`/`Col` map. Also added a 查看画面 button on Profiles List's Android/Agent Android rows (next to 修改/初始化) opening the same grid scoped to just that profile's bound `serials` via a new `components/ProfileDeviceScreensModal.tsx`, so checking a profile's device pool doesn't require cross-referencing serials against the full Devices list.
- **DingTalk rules fixed to judge effective response, not raw extraction (2026-07-27):** `notifications/rules.py`'s empty-response-streak and same-response-streak rules read `SampleResult.responses` directly instead of going through `select_effective_response()` — the one place CLAUDE.md's own "effective response" note (see below) had never actually been enforced. A profile with LLM response extraction enabled routinely has an empty *raw* extraction (nothing directly copyable in the UI) that the LLM review recovers real text from; that pattern was tripping both rules as false-positive DingTalk alerts. Both now share a `_effective_response()` helper that calls `select_effective_response()`.
- **`empty_response_only` excludes `end_session` no-ops (2026-07-27):** `Sample.end_session=true` never sends anything (see "Session-level device stickiness" above — it just releases a device-session pin) so it always looks raw-empty and has no LLM response to recover it, but it's a deliberate no-op, not a real empty-response anomaly. `storage/batches.py::_is_empty_response_clause` now also excludes rows whose `metadata_json` has a `session_released` key (the one signal that branch's `SampleResult` always sets).
- **Multi-turn conversations linked by session_id (2026-07-27):** `Sample.session_id` existed only as an in-memory device-pinning hint during execution (`devices/pool.py::DevicePool`) and was discarded afterward — no way to reconstruct which requests belonged to one multi-turn conversation once it was split across separate requests (each `new_session=false` continuation is typically its own single-sample batch, not one batch with many samples). Added a `session_id` column on `Sample` (+ Alembic migration) stamped onto every `SampleResult` uniformly in `batch_scheduler.py::run_one` (covers every branch, not just the executor-run path); `storage/samples.py::list_samples_by_session_id` queries across the whole table ordered by time; new `GET /api/v1/batches/sessions/{session_id}`; `BatchSummary.session_id` (single-sample batches only) drives a 多轮对话 tag on Batches List that opens `SessionConversationModal`, showing the full reconstructed thread with links to each turn's sample detail page. Generating this migration surfaced real pre-existing schema drift — see next entry.
- **Alembic baseline drift backfilled (2026-07-27):** `Batch.samples_request_json` and three indexes (`Batch.status`, `Sample.status`, `Sample.target_profile`) were added to `models/db.py` in prior pre-Alembic work but never got a real migration, so any DB that went through the actual migration chain (as opposed to the fresh-DB `create_all()` fast path, which always matches `models/db.py` exactly) was missing them — caught via `alembic check` while generating the session_id migration above. Backfilled in its own migration ahead of the session_id one, guarded with existence checks (`sa.inspect(bind).get_columns/get_indexes`, not a plain `op.add_column`/`create_index`) since a *real* legacy (pre-Alembic) DB already has these — they were added by the old hand-rolled PRAGMA-table-info migration code before the Alembic switch — so applying them unconditionally during `init_db()`'s "stamp to baseline, then upgrade" path for such a DB would hit "duplicate column"/"index already exists".
- **`/v1/chat/completions` and `/api/v1/tests/sync` log unexpected failures (2026-07-27):** `openai_compat.py`'s catch-all `except Exception` returned a generic `{"error": "internal execution failure"}` with **zero logging** — a real crash anywhere in the handler (profile resolution, executor, response building) was completely unobservable server-side; only the generic message ever reached anyone. Added a module logger and `log.exception(...)` (captures the traceback) before building the error response, plus `log.warning(...)` for the narrower `SyncSampleResultMissingError` case (both here and in the sibling `/api/v1/tests/sync` handler in `api/tests.py`, which had the identical gap).
- **会话占用 panel layout fix (2026-07-27):** the popover's table rendered every active session at once with no pagination, and `formatRemaining` used "X 分 Y 秒" which line-wrapped across 4 lines in the 剩余时间 column. Widened the panel 560px → 760px, added `Table`'s built-in pagination (6/page, `hideOnSinglePage`), and switched the time format to compact `mm:ss`.
- **Runtime log viewer page (2026-07-28):** there was no way to view the app's own runtime log from the UI at all — `utils/logging.py::configure_logging()` only logs to stdout; the actual on-disk log (`Settings.log_file`, default `logs/uvicorn.log`) is written by `run.sh`'s `>>"$LOG_FILE" 2>&1` redirect, unrotated. Added `GET /api/v1/system/log` (tails the last N lines via `utils/tail.py`'s seek-from-end reader — never loads the whole file into memory, since it has no rotation and could be huge) and `GET /api/v1/system/log/download` (full-file streaming download). New "运行日志 Logs" nav entry under 系统 renders it with `LogViewer` (`components/LogViewer.tsx`) — the same Monaco engine VSCode uses, read-only, with a custom "autoagent-log" Monarch language/theme highlighting timestamps/ERROR/WARNING/INFO/DEBUG/traceback frames. Extracted the Monaco bootstrap (loader config + worker wiring) that used to live inline in `YamlEditor.tsx` into shared `components/monacoSetup.ts`; `LogViewer` is lazy-loaded (`React.lazy`/`Suspense`, matching how `YamlEditor` was already lazy-loaded) so the ~4MB Monaco chunk isn't paid for by pages that don't need it — this was a real regression caught before shipping: an initial version statically imported `LogViewer` from the always-loaded `App.tsx` route tree, which would have pulled Monaco into every page's load.
- **Stale dynamic-import chunk auto-reload (2026-07-28):** a browser tab left open across a deploy still has the previous build's chunk hashes baked into its `React.lazy()` calls (`YamlEditor`/`LogViewer`) — once `static/assets/` is overwritten by the new build, fetching those old-hashed files 404s with "Failed to fetch dynamically imported module" (reported after a production restart, first surfaced by the `LogViewer` chunk added above). Added `web/src/utils/staleChunkReload.ts`, wired into `main.tsx`: listens for Vite's `vite:preloadError` event and reloads once (a `sessionStorage` guard, cleared on every successful boot, prevents a reload loop if the deploy is genuinely broken) instead of leaving the user stuck on `ErrorBoundary`'s manual 刷新 button.
- **`empty_response_auto_reinit` — rule 1 gets the same auto-reinit rule 2 already had (2026-07-28):** `notifications/rules.py::_maybe_auto_reinit()` used to hardcode reading `same_response_auto_reinit` — generalized to take the config key as a parameter so the empty-response-streak rule (rule 1) can opt in independently via its own new `empty_response_auto_reinit` flag (deliberately not shared with rule 2's flag — the two anomalies aren't the same thing, a user may only trust auto-recovery for one). Config page has a matching toggle next to 连续空响应阈值; the alert text gets the same "自动复位" line rule 2's already has.
- **Batches page UI polish batch (2026-07-28):** several small independent frontend improvements, each its own commit — runtime log viewer gets a client-side level filter + search box (`utils/logFilter.ts`, groups log lines into records so a level filter never severs a traceback from its ERROR line); Config page's 钉钉通知/白名单 tabs reorganized into labeled sections (`RuleSection`) instead of one flat 12+-field form; fixed `useDeviceStream`/`useDeviceHttpStream` (`api/deviceStream.ts`) double-closing their `VideoDecoder` on unmount (effect cleanup and the async read loop's own finally/onclose handler both called `.close()` — added a shared `safeCloseDecoder()` guard), which was surfacing as a frequent "Cannot call 'close' on a closed codec" error when viewing device screens; Dashboard's 进行中/排队中 panel used to fetch "6 most recent batches of any status" then filter client-side, silently under-reporting active batches whenever the newest overall batches weren't the same as the actually-active ones — now issues separate `status=running`/`status=queued` queries and the header count comes from `/batches/stats` regardless of how many rows the panel can render; Profiles List's 绑定设备 column decluttered (device chips get their own row, 修改/初始化/查看画面 collapse to an icon-only toolbar below); Batches List gained a device-chip click-through to `DeviceStreamModal`, a toggleable 响应(单条) column showing single-sample batches' effective response (backend already computed this via `select_effective_response` for `preview_response`, just never surfaced it), and `BatchPromptModal`'s quick-look now includes each sample's `ScreenshotStrip`.
- **DingTalk rule 3 — ANR detection (2026-07-28):** unlike rules 1/2 (which only fire for a cleanly `done` sample), this runs after every `gui_android` sample that actually reached a device — including `failed`/`timeout`/`extraction_failed` — since an ANR is a plausible *cause* of those. Detection is a single `adb logcat -d -b system ActivityManager:I *:S` grep for `"ANR in <profile.package>"` (`devices/adb.py::logcat_anr_check`) — Android's own ActivityManager always logs this when its watchdog fires (input-dispatch/broadcast/service timeout), even on OEM builds that suppress the user-facing dialog, so it's a real system-level signal rather than a heuristic; deliberately does *not* attempt to detect a pure UI freeze that never trips Android's own ANR watchdog (e.g. a hung WebView where the native Activity shell itself isn't blocked) — out of scope by design, chosen over heavier screenshot/UI-tree-diff or active-tap-probe approaches per explicit user request to keep the per-sample check cheap. The buffer is cleared after every check (hit or not), making each call a "since last check" window instead of needing to reconcile the device's clock against the server's for a time-filtered query. A hit always runs the profile's init playbook immediately (`notifications/rules.py::_start_reinit_job`, factored out of `_maybe_auto_reinit` so rule 3 doesn't need a separate opt-in flag the way rules 1/2's `*_auto_reinit` do — enabling `anr_check_enabled` at all is the consent) plus a DingTalk alert. `agent_android` is out of scope since that profile type has no `package`/`init_action` (free-roaming LLM agent, same restriction `_maybe_auto_reinit` already applied).
- **Second UI/UX polish batch (2026-07-28):** six more independent fixes, each its own commit — Config page's 已有备份 list gained 下载/删除 per row (`GET`/`DELETE /config/backup/{,download/}{name}`, path-traversal guarded via `maintenance/backup.py::resolve_backup_path`); 重复响应白名单 gained a Profile filter + pagination (was a flat unpaginated cross-profile list); Devices page's 会话占用 panel's 占用设备 Tag had no width constraint and could overflow into 剩余时间 — fixed with `ellipsis: true` + explicit overflow/text-overflow/white-space styling; `BatchPromptModal`'s Prompt/Response labels were small muted text that blended into the content — now bold with an accent color; Batches Detail's 返回 link hard-navigated to a bare `/batches`, discarding whatever filters/pagination/search the List page's querystring held — now prefers `navigate(-1)` (real browser-history back) and only falls back to a plain `/batches` when `location.key === 'default'` (nothing to go back to, e.g. a deep link straight into the detail page); `test-utils.tsx`'s `renderWithProviders` gained optional `initialEntries`/`initialIndex` to let tests exercise that history-back behavior.
- **SampleDetail: new_session bug + Profile row + on-demand log viewer (2026-07-28):** `SampleResult` never persisted `new_session` at all (only the request-side `Sample` had it), so SampleDetail's "New session" field always read `undefined` and displayed `False` regardless of what was actually submitted — added a real `new_session` column on `Sample` (Alembic migration, existence-guarded + `server_default` since SQLite can't add a `NOT NULL` column to a non-empty table without one) and stamped it in `batch_scheduler.py` the same way `session_id` already is. Also added a `Profile` row to the info `Descriptions` block (previously only shown as small muted subtitle text) and an on-demand (collapsed by default) 日志文件 viewer for `executor.log`/`after_result_*.xml` — new `GET .../samples/{id}/artifacts` (lists them) and `GET .../samples/{id}/artifact/{name}` (raw text, name restricted to that allowlist) endpoints, since these were previously only reachable via the full `logs.zip` download.
- **Third UI/UX polish batch (2026-07-28/29):** eight more independent items, each its own commit. `SessionConversationModal` (多轮对话) never got the screenshot/bold-label treatment `BatchPromptModal` did — now shares `ScreenshotStrip` and the same label styling. Rule 2's whitelist gained a symmetric blacklist (`notifications/blacklist.py`, thin wrapper over new shared `_response_list.py` like `whitelist.py` now is) — a blacklisted response skips the streak wait *and* the VLM judge entirely and alerts on the first repeat (checked before the VLM-config gate, so it works with no VLM configured at all); both lists gained manual add (`.../whitelist/add`, `.../blacklist/add` — whitelist was previously delete-only) via a shared `ResponseListPanel`. New `useResizableColumns` hook (drag handles on any column with an explicit numeric `width`, localStorage-persisted per table) wired into Batches List/Detail, Devices List/session table, and Profiles List. `/batches` and `/batches/stats`' `status`/`mode` params became `list[...]` (repeated query params, `.in_()` instead of `==`) so Batches List's filters can multi-select; the URL encodes selections as a comma-joined string. `BatchSummary.is_end_session` (total==1 batches whose sole sample was a `Sample.end_session=true` no-op — reuses `storage/batches.py::_is_end_session_noop_clause`, already used by `empty_response_only`) is now a 结束会话 tag plus a `exclude_end_session` filter. New `storage/samples.py::avg_duration_by_profile()` (`AVG(duration_ms)` grouped by `target_profile`) surfaced as `ProfileSummary.avg_duration_ms` on Profiles List, and reused for `BatchSummary.is_duration_anomaly` (total==1 batches whose own `avg_duration_ms` is `>2x`/`<0.5x` their profile's historical average — `ANOMALY_HIGH_RATIO`/`ANOMALY_LOW_RATIO`, shared by a SQL clause and a plain-Python twin so the new `duration_anomaly_only` filter and the red/bold 耗时 column display flag can't drift apart) plus a shared `web/src/utils/duration.ts` formatter.
- **Blacklist auto-add on confirmed VLM anomaly (2026-07-30):** rule 2's blacklist (added above) only supported manual add — a real VLM-confirmed anomaly (`judgement.normal is False` in `notifications/rules.py::_rule_same_response_streak`) fired the DingTalk alert but never blacklisted the response, unlike the normal-judgement branch which already auto-whitelists. Added a symmetric `blacklist.add(profile, response)` call right before firing the alert in that branch, so the next occurrence of the same response skips the streak wait and VLM judge entirely (via the existing blacklist-hit check earlier in the function).
- **Resizable columns: every border draggable + fixed reversed-direction bug (2026-07-30):** `useResizableColumns` (added in the third UI/UX polish batch above) only attached a drag handle to columns with an explicit numeric `width`, leaving the border next to any auto/flex-width column (e.g. Batches List's `名称` column) completely undraggable. Worse, under the default `table-layout: auto`, the browser recomputes column widths from cell content on every render, silently fighting the widths we set via `<col>` — dragging could visually move the wrong border, or the one you dragged the wrong direction, as unconstrained neighboring columns compensated. Fixed by giving unsized columns a real default pixel width (`DEFAULT_FLEX_WIDTH = 260`, so every border is resizable) and having the hook also return a `scroll.x` (sum of all column widths); every call site (Batches List/Detail, Devices List/session table, Profiles List) now also passes `tableLayout="fixed"` so widths are strictly honored instead of recomputed. Side effect worth knowing: `tableLayout="fixed"` + `scroll.x` turns on rc-table's internal `measureColumnWidth` (hidden `aria-hidden` "measure row" in `<tbody>` mirroring header titles, used to compute pixel widths) — tests asserting on header text via `within(table)` must scope to `table.querySelector('thead')` or they'll get a duplicate-match error against that hidden row.
- **SSE reconnect now replays missed events instead of dropping them (2026-07-30):** `events/bus.py::BatchEventBus.replay_since()` existed for exactly this since the reconnect-storm fix above, but the `/{batch_id}/events` route never called it, and `useBatchStream` never retried a dropped connection at all — a network blip or backend restart mid-batch left the UI's progress silently frozen until a manual page reload, with no way to recover the events published during the gap. The route now accepts `?after_seq=`, replaying `bus.replay_since()` before falling into the live subscription (subscribes *before* reading the replay buffer so nothing published in between is lost — the frontend's existing `seq <= seqRef.current` guard harmlessly de-dupes if a replayed event also arrives live). `useBatchStream` now loops: on any stream end that isn't a `batch_done` event or a `404` (genuinely deleted batch), it waits `RECONNECT_DELAY_MS` (1.5s) and reconnects with `after_seq` set to the last seq it actually applied. Caught a real sse_starlette test-infra bug along the way: its module-level `AppStatus.should_exit_event` is created lazily and never reset, so a 3rd SSE test in the same session reused a previous test's now-closed event loop and crashed with "bound to a different event loop" — `test_sse_endpoint.py`'s autouse fixture now resets it after each test.
- **SampleDetail's 动作日志 table joins the resizable-columns pattern (2026-07-30):** the last table in the app not wired into `useResizableColumns` — two of its four columns also had no `key`/`dataIndex` at all (the hook needs one to track a per-column width), so they were quietly unresizable-by-construction even before the fix above.
- **Eighth small UX/stability batch (2026-07-31):** three more independent fixes, each its own commit — the last round before this class of bug appeared genuinely exhausted after 8 passes over the codebase. `WebExecutor._get_or_create_session`'s browser context was only ever referenced from `self._sessions`, set after `new_page`/`goto`/`wait_for_selector`/`screenshot` all succeed — a failure anywhere in that setup (bad URL, selector timeout) raised without closing the already-launched context, leaking a live browser process for good on every failed attempt; now closed in an `except` before re-raising. `batch_scheduler.py`'s webhook delivery checked `send_webhook`'s exceptions but never its bool return (all retries exhausted) — a permanently-failed callback had no summary log tying the failure back to which sample it was for, just the generic per-attempt warnings already inside `send_webhook` itself. Dashboard's onboarding checklist used `stats?.total ?? 0`, so it briefly flashed for every user (including ones with plenty of existing batches) on every page load until `useBatchStats` actually resolved — now gated on `stats !== undefined`.
- **Failure reason clustering (2026-08-03):** Batches Detail gained a 错误分组 panel above the sample table (collapsed by default, only rendered when a batch has 2+ failed samples) that groups failures by *normalized* `error` text — count + one example per group, sorted by count descending — and lets clicking a group's 筛选 button narrow the table to just that group's samples, composing (AND) with the existing status filter and free-text search rather than replacing them. Pure frontend, no backend change: `web/src/utils/failureClustering.ts::groupFailuresByError` reads the same `Batch.samples` array Batches Detail already has loaded. `error` is `str(exception)` and routinely embeds run-specific values (device serial, timeout ms, tap coordinates, hex ids, timestamps) that would otherwise fragment one real root cause into many "unique" groups — `normalizeError` strips exactly those patterns (via a fixed, narrow regex list, in a specific order so e.g. the ms-timeout rule consumes its digits before the seconds-duration rule can also match) down to placeholder tokens, then groups by exact string match of the result — deliberately not a fuzzy-similarity/threshold approach (no new dependency, fully deterministic, and the normalized string doubles as a human-readable explanation of *why* two errors were grouped). Deliberately leaves quoted locator/selector content untouched (two "element not found" errors for two different selectors are a real distinction, not noise) and requires the hex-id rule's match to contain at least one digit (via a lookahead) so a plain English word that happens to be all a-f letters, e.g. "facade", isn't mistaken for an id. Scoped to one batch's own samples; cross-batch clustering is a deferred follow-up. Design: `docs/superpowers/specs/2026-08-03-failure-clustering-design.md`; plan: `docs/superpowers/plans/2026-08-03-failure-clustering.md`.
- **Deploy fix — install frontend deps before building (2026-08-04):** `run.sh`'s `build_spa` and `system/updater.py`'s apply flow both ran `pnpm build` without ever running `pnpm install` first, so a `git pull` that added a frontend dependency (e.g. the `diff`/jsdiff dep from the batch-comparison feature) left `node_modules` stale and the build failed with `Cannot find module 'diff'` / `error TS2307: Cannot find module`. Both now install first: `run.sh` runs `pnpm install --frozen-lockfile && pnpm build` (fast no-op when already in sync); the self-updater gates a `pnpm install --frozen-lockfile` on `web/package.json`/`web/pnpm-lock.yaml` appearing in the pulled diff (`_frontend_deps_changed`, mirroring the existing `_deps_changed`/`uv sync` gate) and aborts the whole apply before restarting if it fails. Manual server recovery for a box already stuck in this state: `cd web && pnpm install` (pnpm, not npm — the repo has a `pnpm-lock.yaml`, and `npm install` errors with `Cannot read properties of null (reading 'matches')`) then `./run.sh build`.
- **Response full-text search (2026-08-05):** a sample-level, cross-batch keyword search over *response* content — the batch-list `q` only ever matched prompts (`prompts_sent_json`), so there was no way to answer "which samples' responses contained '抱歉我无法'". Pure retrieval, no content judgement (consistent with the tool's positioning: it *obtains* responses; judging them is left to other platforms). Backend `storage/samples.py::search_samples_by_response(q, target_profile=None, *, limit, offset)` LIKEs `responses_json` OR `llm_responses_json` (escaped via a local `_like_term` — a copy of `batches.py`'s, since importing it back would be circular; Chinese matches because both columns store `ensure_ascii=False`), newest-first, paginated, and extracts a `_snippet` (±40-char context around the match) + `source` (`response`/`llm_response`, whichever array contains the term). New `api/search.py` router → `GET /api/v1/samples/search?q=&target_profile=&limit=&offset=` (`q` min_length=2 to block whole-table scans, `limit` ≤200). Frontend: a 任务→"响应搜索" page (`pages/Search/ResponseSearch.tsx`, route `search/responses`, `SearchOutlined` nav) with an `Input.Search` (commits on submit, not per-keystroke), an optional profile filter, pagination, and a `<mark>`-highlighted snippet via a pure `utils/highlight.ts::splitHighlight` (regex-escapes the term, case-insensitive); each row shows source/status tags and a 查看 link to the sample detail. `useSampleSearch` is disabled below 2 chars so it never fires an empty search. Design: `docs/superpowers/specs/2026-08-05-response-search-design.md`; plan: `docs/superpowers/plans/2026-08-05-response-search.md`.
- **Health trends (2026-08-04):** the Profile Health dashboard (a current 7-day snapshot) gained time-series trends so a profile that's *drifting* worse is visible before it crosses a threshold. New `storage/samples.py::daily_stats_by_profile(since)` buckets terminal-executed samples by `(target_profile, date(ended_at))` (SQLite `strftime('%Y-%m-%d')`) into per-day `DailyPoint`s (success_rate = done/total, avg_duration_ms, sample_count); only days with samples appear (no zero-fill). One batch endpoint `GET /api/v1/profiles/trends?days=30` (≤90) returns `{profile: DailyPoint[]}` — serves both the card sparklines and the modal (the modal just reads `trends[name]` from the already-fetched batch, no per-profile request). Frontend added **recharts** as a dependency, but **lazy-loaded** (`React.lazy` on `ProfileSparkline`/`ProfileTrendModal`, mirroring the Monaco split) so it lands in its own chunk (`LineChart-*.js` ~291KB + the modal chunk) loaded only when the Health page renders — the main bundle stays lean, verified in the build output. Each health card shows an inline success-rate sparkline (`ProfileSparkline`, minimal axis-less `LineChart`) and is now clickable → opens `ProfileTrendModal` with three larger `LineChart`s (成功率 / 平均耗时 / 样本量, with axes + tooltip); the card's existing name/anomaly/device links `stopPropagation` so they don't also open the trend modal. Fixed daily granularity, no configurable per-metric windows (deferred). Design: `docs/superpowers/specs/2026-08-04-health-trends-design.md`; plan: `docs/superpowers/plans/2026-08-04-health-trends.md`.
- **Profile health dashboard (2026-08-04):** a dedicated 资源→"Profile 健康" page (`web/src/pages/Profiles/Health.tsx`, route `profiles/health`, nav entry with `HeartOutlined`) showing every profile as a health card — a worst-of status block (🟢/🟡/🔴/⚪) over four signals scoped to the last 7 days: success rate, avg duration (display-only, doesn't drive color), unacked anomaly count, and device-pool online/total. Backed by `GET /api/v1/profiles/health` → `list[ProfileHealth]`, assembled in `src/autoagent/health/profile_health.py::list_profile_health` from three new windowed aggregates (`storage/samples.py::success_stats_by_profile(since)` — done/terminal counts, terminal = done/failed/timeout/extraction_failed; `avg_duration_by_profile` gained an optional `since` param, default None = all-time so existing callers are unchanged; `anomalies/store.py::unacked_counts_by_profile(since)`) plus live `list_devices()`. `compute_health` is a pure worst-of function: success `<0.5`→red/`<0.9`→yellow; unacked anomalies `>=5`→red/`>=1`→yellow; device pool (only for `android`/`agent_android` profiles with bound serials — note it's the profile `platform` field literal `"android"`, NOT the `gui_android` *Sample.mode*) `0` online→red/partial→yellow; `total_runs==0`→⚪ nodata; overall = worst participating signal (avg duration deliberately excluded — slow≠unhealthy, and duration outliers already flow in via the anomaly count). Backend sorts worst-first; the page renders that order verbatim (no client-side re-sort — single source of truth). Cards click through: name→profile edit, anomaly count→`/system/anomalies?target_profile=<name>` (the Anomaly Center now reads `target_profile` from the URL and prefills its filter), and (android/agent_android only) the device x/y count→`ProfileDeviceScreensModal` scoped to that profile's serials (`ProfileHealth` exposes the bound `serials` for this). A summary bar + "只看非健康" toggle. First cut is these four signals; response-length / batch-success-rate detectors and trend charts are deferred. Design: `docs/superpowers/specs/2026-08-04-profile-health-dashboard-design.md`; plan: `docs/superpowers/plans/2026-08-04-profile-health-dashboard.md`.
- **Anomaly center enhancements (2026-08-04):** three additions to the anomaly center. (1) **Time-range filter** — `list_anomalies`/`GET /api/v1/anomalies` gained `created_after`/`created_before` params, surfaced as an AntD `RangePicker` (presets: 最近 1 小时/24 小时/7 天/30 天) on the Anomalies page. (2) **Historical backfill** — `anomalies/backfill.py::backfill_duration_anomalies` scans every profile's past timed samples through the *same* IQR duration detector the live hook uses, **point-in-time** (each sample judged against the samples before it, respecting `MIN_HISTORY`/the 500 recency cap), so backfilled anomalies share the live IQR contract; idempotent via `store.existing_duration_sample_ids()` (each sample → at most one duration anomaly, so re-running or overlapping with live-created records never duplicates); profile set is **sample-derived** (`storage/samples.py::distinct_sample_profiles`, not on-disk YAMLs — mirrors the live per-sample hook, covers deleted-profile history). Exposed as `POST /api/v1/anomalies/backfill` (synchronous — pure iteration + bounded inserts) + a "扫描历史生成异常" button on the Config page. This is the fix for "freshly-deployed anomaly center is empty" — the live detector only fires going-forward and never backfilled. (3) **Periodic DingTalk digest** — opt-in `DingTalkNotificationConfig.digest_interval_hours` (0=off); `maintenance/scheduler.py::run_digest_loop` (wired into `main.py` lifespan alongside `run_backup_loop`) ticks hourly and, when `digest_interval_hours` has elapsed since the last send, DingTalks a summary of anomalies created since the last digest (`store.anomalies_created_since` + pure `anomalies/digest.py::build_digest_markdown` — grouped counts by type/profile + top examples). Zero new anomalies → skips the send but still advances the `anomaly_digest_state` kv timestamp (no "all clear" spam, window stays bounded); a failed send does NOT advance (retries next tick). Design: `docs/superpowers/specs/2026-08-04-anomaly-center-enhancements-design.md`; plan: `docs/superpowers/plans/2026-08-04-anomaly-center-enhancements.md`.
- **Anomaly center (2026-08-03):** a unified in-app anomaly view replacing the previously fragmented/ephemeral detection surface (the three DingTalk rules pushed to chat only, leaving no in-app trace; the sole in-app signal, `is_duration_anomaly`, was a display-only fixed-ratio flag). New `anomalies` DB table (+ Alembic migration) written to by two sources: (1) a new statistical duration detector — `anomalies/duration_detector.py::evaluate_duration` flags a sample whose `duration_ms` falls outside its profile's IQR fences (`Q1−1.5·IQR` / `Q3+1.5·IQR`), computed over the profile's most recent 500 timed durations (`storage/samples.py::recent_durations_for_profile`; SQLite has no percentile function so the list is pulled and IQR computed in Python via `statistics.quantiles`), with a ≥20-sample min-history guard so a thin baseline never fires; runs at sample completion in `batch_scheduler.run_one` right after the notify hook, and is panel-only (no new DingTalk noise, matching duration's always-push-free nature). IQR (Tukey fences) chosen over z-score because latency is right-skewed — mean±std over-flags. (2) The three existing DingTalk rules (`notifications/rules.py`: empty-streak, same-response, ANR) now *also* write a record (best-effort via `_safe_record_anomaly`, so a DB hiccup can't break an alert) alongside their unchanged push. Single write/read/ack layer in `anomalies/store.py`. New `api/anomalies.py` router (`GET /anomalies` list+filter by type/profile/acknowledged, `GET /anomalies/count` for the nav badge, `POST /anomalies/{id}/acknowledge` idempotent + 404). Anomalies cascade-delete with their batch (`storage/batches.py::delete_batch_rows` gained a `DELETE FROM anomalies WHERE batch_id=?`) so they inherit the batch's retention lifecycle — no separate prune job. Frontend: a 异常中心 nav entry under 系统 with an unacknowledged-count Badge (`useAnomalyCount`), a `pages/System/Anomalies.tsx` table (type/profile/acknowledged filters, per-row + checkbox-bulk 确认 via `Promise.allSettled`, click-through to each anomaly's sample detail). The existing Batches List `is_duration_anomaly` ratio flag is deliberately left as-is (a fast at-a-glance hint; the center is the deeper statistical view — possible future convergence, not touched this iteration). First cut is duration-only; response-length and batch-success-rate detectors are deferred. Design: `docs/superpowers/specs/2026-08-03-anomaly-center-design.md`; plan: `docs/superpowers/plans/2026-08-03-anomaly-center.md`.
- **Batch comparison (diff) view (2026-08-03):** Batches List's existing checkbox selection gained a 对比 action (shown only when exactly 2 batches are selected) opening `/batches/compare?a=<id1>&b=<id2>` — a new `web/src/pages/Batches/Compare.tsx` page that fetches both batches via the existing `useBatch` hook (no backend change), aligns their samples by `Sample.id` (stable across rerun/replay and across resubmitting the same JSON batch definition — the two natural "compare two runs" scenarios), and renders an AntD `Table` with a per-sample duration delta (Δ = B − A, danger-red when B is slower, success-green when faster — directional, distinct from Batches List's magnitude-only duration-anomaly highlight) plus, on expand, a word-level highlighted diff of each side's *effective* response. Alignment/effective-response logic is a pure function (`web/src/utils/batchComparison.ts::buildBatchComparison`) reusing `selectEffectiveResponseText` so this view can't drift from what every other "what response did this sample actually produce" surface shows (the exact drift class that's bitten this codebase before). Only the first prompt round is compared (multi-round comparison is a deferred non-goal); samples present in only one batch are called out with a 仅 A/仅 B tag and aren't expandable. Word-level diffing uses the new `diff` (jsdiff) dependency via a small theme-aware `web/src/components/DiffText.tsx` (added = success bg, removed = error bg + strikethrough, colors from AntD tokens so it reads correctly in dark mode), computed lazily only when a row is expanded. Design: `docs/superpowers/specs/2026-08-03-batch-comparison-design.md`; plan: `docs/superpowers/plans/2026-08-03-batch-comparison.md`.
- **Sample replay timeline — shipped 2026-07-31, reverted 2026-08-03:** SampleDetail's screenshot strip + `action_log` table were briefly replaced with one scrubbable timeline (big image + AntD `Slider` scrubber merging screenshots/action-log by elapsed time, via `web/src/utils/replayTimeline.ts::buildTimelineEvents`). Reverted at explicit user request — the original flat thumbnail grid (click-to-zoom) plus the action log table were preferred over the slider interaction. `SampleReplayTimeline.tsx`/`.test.tsx` and `replayTimeline.ts`/`.test.ts` were deleted as dead code; SampleDetail is back to `<ScreenshotStrip>` + the 动作日志 `<Table>`. Design/plan docs (`docs/superpowers/specs/2026-07-31-sample-replay-timeline-design.md`, `docs/superpowers/plans/2026-07-31-sample-replay-timeline.md`) are left in place as history but no longer describe current behavior.
- **Ninth small UX/stability batch (2026-07-31):** two more independent fixes, each its own commit. Web Profile Builder's backend session dict has no TTL/idle reaper — its Playwright browser process only ever got closed via an explicit close-session call (save or the close button), so abandoning the page any other way (sidebar nav, back button, tab close) leaked that process indefinitely; now closed in an unmount effect too (safe to double-close, 404s harmlessly). `deviceStream.ts`'s `VideoDecoder` `error` callback (both `useDeviceStream` and `useDeviceHttpStream`) only did `console.error` — per the WebCodecs spec a decode error closes the codec, but `state` never reflected it, so the canvas silently froze on the last frame with no error indicator and no way to reach the existing manual-reconnect affordance that `DeviceStreamModal` already renders for `state === 'error'`.
- **`avg_duration_ms` miscounted end_session no-ops as duration anomalies (2026-07-31):** user-reported bug, not found via exploration. `batch_scheduler.py::run_one`'s `avg_duration_ms = total_duration_ms // (done_count + failed_count)` — `total_duration_ms` correctly skipped samples with no `duration_ms` (end_session no-ops, cancelled, device-acquisition failures) when summing, but the denominator didn't exclude them, so each one diluted the average as if it were a legitimate 0ms sample. A pure end_session single-sample batch computed `avg_duration_ms=0`, which then falsely tripped Batches List's "batch average far below profile average" duration-anomaly flag/filter (`storage/batches.py::is_duration_anomaly`/`_duration_anomaly_clause`, both already correctly guard on `avg_duration_ms is None` — they just never got the chance since the buggy computation produced `0` instead of `None`). Fixed by tracking a separate `timed_count` in `_RunState`, dividing by that instead — `avg_duration_ms` now stays unset (`None`, via `update_batch_progress`'s existing "`None` = don't touch this field" semantics) rather than `0` when nothing in the batch was ever timed, which was sufficient to fix both the flag and the filter with no changes needed there.
- **Seventh small UX/stability batch (2026-07-30):** six more independent fixes, each its own commit. `DeviceStreamModal`'s `postDeviceInput` calls (tap/swipe/key/text) only did `.catch(console.error)` — a device going offline mid-session made input silently vanish with the canvas still showing "直播中"; now shows an error toast. `devices.py`'s `install_adb_keyboard_for_device`/`enable_ime_route`/`disable_ime_route` each guarded their main adb action against `AdbCommandError` but not the status-reread call built into the same response (`is_package_installed`/`is_ime_enabled` right after) — a device dropping offline between the two adb calls 500'd instead of a clean 502; now both calls share the same guard. `POST /{batch_id}/rerun` and `/replay` never checked the original batch's own status before resubmitting — rerunning/replaying a batch still `queued`/`running` drove the same profile/devices from two batches at once; now rejected with 409. `DeviceInitModal` never checked `useInitJob`'s `isError` — failed polling (backend restart drops the in-memory job) left the modal showing stale per-device tags forever with no indication the status feed died. `SystemUpdatePanel`'s `disabled` defaulted to `false` when the status fetch itself failed, leaving apply (RCE-by-design, gated behind `self_update_enabled`) clickable with the version tag just showing "未知" as if that were normal — now fails closed and shows an error alert. `formatDurationMs` had no NaN/negative guard, rendering the literal string `"NaNm NaNs"` in Profiles List/Batches List's duration columns.
- **Sixth small UX/stability batch (2026-07-30):** four more independent fixes, each its own commit. Devices List's per-row 启用/禁用 button read `connectDevice.isPending`/`disconnectDevice.isPending` directly — both shared across every row since it's one mutation hook for the whole table, so toggling one device made every other row's button spin/disable too; now tracks which serial is actually in flight. `maintenance/scheduler.py::run_backup_loop`'s second `_current_backup_config()` call (sizing the next sleep) sat outside the try/except wrapping `_backup_tick_once()` — a transient failure there (e.g. a DB hiccup) killed the loop's task permanently, unlike `run_retention_loop`'s equivalent read, which was always inside its own guarded tick; no more backups until a process restart otherwise. `useBatchStream`'s SSE reconnect loop already stopped cleanly on a 404 (batch deleted while its detail page was open) but never surfaced that — the page just kept showing stale cached state forever, indistinguishable from a batch still being live-tracked; the hook now exposes a `streamGone` flag and Batches Detail shows a dismissible warning banner with a manual refresh button. `SessionConversationModal` never checked `isError` — a failed fetch rendered the same "没有找到属于这个会话的记录" empty state as a genuinely empty session.
- **Fifth small UX/stability batch (2026-07-30):** seven more independent fixes, each its own commit. A mid-session 401 (expired token) used to hard-redirect straight to `/login` with zero explanation — the redirect destroys the whole React tree, so `api/client.ts`'s response interceptor now sets a `sessionStorage` flag right before redirecting and `Login` shows "登录已过期，请重新登录" once on mount if it finds it, then clears it. Profiles Edit gained a dirty-draft guard (`beforeunload` + a `Modal.confirm` on the 配置档 breadcrumb) — neither a tab close nor that breadcrumb link warned before discarding unsaved YAML. Profile Builder's `startSession` used a native `window.confirm()` — the one remaining spot not using AntD's own Modal/Popconfirm — replaced with `modal.confirm` (via `App.useApp()`, not the static `Modal.confirm`, which in testing turned out to leak its portal across renders and cause flaky multi-match failures in a later test). Config page's whitelist/blacklist 刷新 buttons had no `loading` prop at all (double-click fired duplicate requests with no feedback). `profile_builder.py` (no logger at all) and `web_profile_builder.py` (a `_log` defined but never actually called) returned a 502 on browser-launch/device-connect/capture failures with zero server-side trace — same class of gap already fixed for `openai_compat.py`/`tests.py`. Batches New's failed-submit errors moved from a transient `message.error` toast to a dismissible `Alert` above the form, since the toast could disappear before anyone read a detail like `sample s2 mode=api != batch mode=gui_android` naming which row was wrong (the backend returns a plain string, not per-field errors, so this doesn't drive `Form.Item`-level validation — just keeps the message from vanishing).
- **Fourth small UX/error-handling batch (2026-07-30):** four more independent fixes, each its own commit. Config page: `saveErrorMessage` read `error.response.data.detail`, but errors reaching a mutation's catch block are already-normalized `ApiError` instances with no `.response` (the shared `api/client.ts` interceptor does that first) — a failed VLM save showed a raw JSON blob instead of "认证失败：<message>"; fixed by parsing `ApiError.detail` (normalizeError JSON-stringifies non-string `detail` values into it) back out. `handleTestLLM` also had no `try`/`catch` around `mutateAsync`, unlike its sibling `handleTestNotify` right next to it — a thrown request (network error/timeout) was an unhandled rejection with zero feedback, button just stopped spinning. `api/client.ts`'s response interceptor now unwraps a JSON-typed `Blob` error body (via `FileReader`, since `Blob.prototype.text()` isn't implemented in the jsdom test environment this ships with) back to text before `normalizeError` runs — requests using `responseType: 'blob'` (results zip, backup download) still get their error body delivered as a Blob even on a JSON error response (axios respects the requested responseType regardless of status), so every failed download previously showed a generic "未知错误" instead of the real reason. Devices List's `runBulk` (批量启用/禁用/删除) used to unconditionally clear the whole checkbox selection after a partial `Promise.allSettled` failure — now narrows the selection to just the devices that failed, so retrying doesn't mean re-picking from scratch.
- **`清除全部` only cleared the last-applied filter (2026-07-30):** Batches List's `clearAllFilters` called eight separate setter functions back-to-back, each independently calling `updateParams()` → `setSearchParams()`'s functional-updater form — all eight run in the same tick and each reads the same stale `prev`, so only the last one's change actually stuck (exactly the race `updateParams`'s own comment already documents, just never applied to this call site). Multi-select itself (status/mode `Select mode="multiple"`) was already correct on its own; this bug just made it *look* broken once `清除全部` was used afterward. Fixed by consolidating into one `updateParams` call.
- **Small UX polish batch (2026-07-30):** five independent items, each its own commit. Devices List's Serial column and Batches Detail's sample ID column gained copy buttons (`Typography.Text copyable`), matching the pattern already used for batch_id/device_serial elsewhere — Serial in particular gets pasted into `adb -s <serial> ...` often enough that this was real daily friction. Profiles List's delete `Popconfirm` gained a `description` spelling out the irreversible consequence and a red `okButtonProps={{danger: true}}`, matching Batches/Devices' delete confirmations instead of being the one bare-title holdout. Batches Detail's sample table now persists its page size to localStorage (`autoagent_batch_samples_page_size`) instead of resetting to 50 on every revisit. Batches List gained `sort_by`/`sort_dir` on `list_batches()`/`GET /batches` (whitelisted to `avg_duration_ms`/`started_at`, `created_at` as a stable tiebreaker) so 耗时/开始时间 can be sorted — deliberately server-side, not an AntD client-side `sorter`, which would only reorder whichever page happened to already be fetched (the same pagination-correctness pitfall `status`/`mode` filtering hit before it moved server-side). Sort state round-trips through the URL (`sort`/`sort_dir` params) like every other filter on this page.
- **Monaco bundle stopped shipping unused typescript/html/css language workers (2026-07-30):** `monacoSetup.ts`'s `import * as monaco from 'monaco-editor'` (the package root barrel) registers every bundled language as a side effect, each with its own worker — `ts.worker` alone was ~7MB unminified, plus `html.worker`/`css.worker`/`json.worker`, none ever actually requested (`getWorker` only ever returned the base editor worker or, for label `'yaml'`/`'json'`, a worker aliased `yamlWorker` that was really the JSON language worker — dead code, since nothing in the app sets Monaco's language to `'json'` and `'yaml'` doesn't have a dedicated worker to begin with). Switched to importing `monaco-editor/esm/vs/editor/editor.api` (core editor only) plus explicitly `monaco-editor/esm/vs/basic-languages/yaml/yaml.contribution` (the lightweight Monarch tokenizer YamlEditor's `defaultLanguage="yaml"` needs — LogViewer's "autoagent-log" language is already registered manually at runtime and needs no bundled contribution). Result: ~9MB of dead worker chunks gone entirely from the build output, and the main monaco chunk itself shrank ~13% (unused language-registration glue no longer pulled in). Needed a `declare module 'monaco-editor/esm/vs/editor/editor.api'` ambient shim in `vite-env.d.ts` — `monaco-editor`'s `package.json` `exports` map only declares a `types` condition for the root specifier, not this deep subpath, so `moduleResolution: "bundler"` can't resolve its types on its own even though the `.d.ts` file sits right there and Vite resolves the JS fine.

## Deferred work for Plan 5

Docker/packaging are still not started (no scope agreed yet). Backups are now covered — see "Data backup (2026-07-23)" above. The security-hardening backlog that used to live here is done — see "Security hardening (2026-07-16)" above.

## Layout

```
alembic/          DB schema migrations (versions/) + env.py (async engine, target_metadata =
                  models/db.py's Base.metadata). alembic.ini's sqlalchemy.url is left unset —
                  env.py fills it in from Settings.data_root at runtime.
src/autoagent/
  api/            FastAPI routers: auth, profiles, tests, batches, config, devices, device_stream,
                  media, system, openai_compat, profile_builder, web_profile_builder
  auth/           JWT + password hashing + static API key (bearer.py) + FastAPI deps
  config/         Pydantic Settings (env-backed)
  devices/        adb wrapper, device pool (concurrency-safe checkout for gui_android/agent_android),
                  monitor (online/offline polling), initializer/init_jobs, expected_state (planned
                  reboot suppression)
  events/         in-process async event bus (per-batch SSE progress)
  executors/      base Executor + api/web/android/agent_pc/agent_android executors, plus support
                  modules: action_runner, ocr, scroll_stitcher, complete_detector, response
                  extraction (ui-tree/OCR/LLM/copy-button-VLM), profile_builder_* generation,
                  agent_core (agent_pc/agent_android's shared loop — see agent runtime unification
                  below; entry point is `agent_loop.py::AgentLoop`, now a thin wrapper around
                  `runtime.py::AgentRuntime` + `parser.py`/`result.py`; `handlers/{pc,android}.py`
                  are the per-platform action handlers)
  loaders/        JSONL/JSON/CSV batch file loaders
  maintenance/    scheduled runtime cleanup (logs/profile-builder artifacts) + batch retention
                  (prunes/archives terminal batches past a retention window) + DB/profiles backup
  models/         Pydantic API schemas + SQLAlchemy ORM
  notifications/  DingTalk webhook sender + rules (e.g. same-response streak detection via VLM
                  judge) + per-profile response whitelist + device state-change alerts
  openai_compat/  request/response schemas + mapping logic for `/v1/chat/completions`
  profiles/       profile Pydantic discriminated union + YAML registry
  results/        per-batch JSONL result writer (thread-safe)
  scheduler/      async BatchScheduler with per-batch concurrency
  services/       shared business logic reused across internal + OpenAI-compat routes
                  (sync_tests.py)
  storage/        async SQLAlchemy CRUD (batches, samples, users, devices, kv config)
  system/         self-update (git pull + uv sync + pnpm build + detached run.sh restart) + hourly
                  fetch loop
  utils/          env var expansion, HTTP retry helper, logging setup, log tail-reader
  webhooks/       async webhook sender with exponential backoff
  main.py         FastAPI app + lifespan (DB init + admin bootstrap + CORS + maintenance scheduler)
tests/
  unit/           ~90 unit test files
  integration/    ~25 integration test files (httpx AsyncClient against the app)
docs/superpowers/{specs,plans}/   Design specs and implementation plans
scripts/          setup.py (installer, called by install.sh), cleanup_runtime_artifacts.py (CLI
                  retention tool), openai_compat_smoke.py
```

## Frontend (Plan 2)

- `web/` — Vite + React + TypeScript + AntD 5. Built into `src/autoagent/static/`.
- State: TanStack Query v5. Batch detail pages now use `useBatchStream` over SSE for live progress; some older list views still use standard query polling where streaming is not needed. Token storage: `localStorage["autoagent_token"]`.
- Dev: `cd web && pnpm dev` (port 5173, proxies `/api/v1` to 8000).
- Build: `cd web && pnpm build` (writes to `src/autoagent/static/`).
- Test: `cd web && pnpm test` (Vitest + RTL).

## Conventions

- **Python interpreter:** prefer `uv run <tool>` (`uv run pytest`, `uv run ruff check .`, `uv run uvicorn ...`) over a bare `python3.11 -m <tool>`. They can silently diverge — CI (and `uv.lock`) pin exact tool versions via the project's `uv`-managed venv, while a global `python3.11 -m ruff`/`pytest` picks up whatever's pip-installed system-wide, which drifted to an older `ruff` that missed 86 real lint errors `uv run ruff check .` caught (2026-07-22 CI investigation, see below). If you do need `python3.11 -m X` for some reason, verify it's resolving to `.venv`, not a system/global install.
- **Tests:** `pytest-asyncio` in `asyncio_mode = "auto"`. Tests live under `tests/unit/` and `tests/integration/`. `pythonpath = ["src"]` is set in `pyproject.toml`.
- **Lint/format:** `ruff` with `line-length=100`, rules `E F I W B UP`. FastAPI routers have per-file-ignore for B008 (`Depends`/`Form`/`File` in defaults are idiomatic). Run `uv run ruff check .` and `uv run ruff format .`.
- **Exception chaining:** use `raise HTTPException(...) from e` when re-raising inside `except` (B904). Don't broaden handlers just to silence lint — narrow the `except` type instead.
- **Secrets in configs:** `admin_password`/`jwt_secret`/`static_api_key` on `Settings` are `pydantic.SecretStr` — access via `.get_secret_value()`, never compare/log the field directly. `repr(settings)`/`str(settings)` print `**********` for these, so it's safe to log the settings object itself, just not an individual unwrapped secret.
- **Result format:** one JSONL file per batch at `<data_dir>/results/<batch_id>.jsonl`. Writer is append-only and thread-safe.
- **DB schema migrations:** `models/db.py`'s `Base` is the source of truth; schema changes are Alembic migrations under `alembic/versions/`, not hand-edits to `storage/database.py::init_db()`. Workflow: edit `models/db.py`, then `uv run alembic revision --autogenerate -m "..."`, review the generated file (autogenerate misses some changes — e.g. column type-only alters on SQLite), commit both together. `init_db()` picks the right path automatically at boot: a fresh DB (every test run, any new install) gets the schema via `Base.metadata.create_all()` directly (fast — no need to replay migration history) and is stamped to head; a pre-existing DB with no `alembic_version` table gets stamped to the baseline revision then upgraded normally, so anything after baseline (e.g. the one-time `ensure_ascii` backfill migration) still actually runs for it; anything already alembic-managed just gets `upgrade head`. Because of the fresh-DB fast path, `models/db.py` and the latest migration must always describe the same schema — don't let them drift.
- **Alembic logging:** `alembic/env.py` deliberately does *not* call `logging.config.fileConfig()` (the Alembic-generated template default) — it defaults to `disable_existing_loggers=True`, which would silently disable every logger `utils/logging.py::configure_logging()` already set up, since `init_db()` runs Alembic on every boot. Cost a real bug once (a `caplog`-based test started failing because its logger got disabled by the *next* test's `init_db()` call) — don't re-add it.
- **Batch rerun vs replay:** `POST /batches/{id}/rerun?status=failed|all` rebuilds samples from persisted `SampleResult` rows (prompts/mode/target_profile only — `new_session`/`timeout_sec`/`retry`/`dry_run`/`callback_url` fall back to defaults, since those aren't in `SampleResult`). `POST /batches/{id}/replay` resubmits the *exact* originally-submitted `Sample` list, verbatim — `BatchScheduler.submit()` persists `Batch.samples_request_json` (JSON dump of the input `Sample` list) at submission time for this purpose. Batches created before this existed have `samples_request_json=None` and `/replay` 400s on them; use `/rerun` instead.
- **Profiles:** YAML files under `<data_dir>/profiles/<name>.yaml`. Names restricted by allowlist regex in `profiles/registry.py::_path`.
- **Playwright verification:** in this environment, real-browser pytest cases may need to run outside the sandbox because Chromium launch is blocked inside the sandbox. When verifying Plan 3 locally, use `python3.11 -m pytest -v` outside the sandbox for the full suite, or `python3.11 -m pytest -q -m "not playwright"` for the fast subset.
- **Android verification:** real-device cases will be marked `@pytest.mark.android`; keep them out of the fast suite with `-m "not android"`.
- **ADB Keyboard:** bundled at `src/autoagent/fixtures/ADBKeyboard.apk`. The `/devices` page shows:
  - **Install** button (if not yet installed on device)
  - **Enable/Disable IME** toggles (if installed)
  - Status tags: `installed`/`not installed`, `ime enabled`/`ime disabled`
  Android execution auto-switches to `com.android.adbkeyboard/.AdbIME` for non-ASCII prompts and restores the previous IME when done.
- **Fake chat fixture APK:** source lives in `tests/fixtures/fake_chat_apk/`; build `fake_chat-debug.apk` with Android Studio or set `AUTOAGENT_FAKE_CHAT_APK` to an externally built artifact before running `pytest -m android`. This fixture is optional; Plan 4 can also be validated manually against a real device + real target app.
- **Android/Profile Builder handoff:** read `docs/superpowers/plans/2026-04-24-android-profile-builder-handoff.md` first when resuming Plan 4. It is the active status note for recent Qwen/Tongyi debugging, ADB Keyboard input fixes, and remaining verification blockers.
- **Android profile semantics:** `new_session_action` is for starting a clean conversation. Input focusing belongs in `input_focus_action`. Send triggering can live in `send_action` when runtime behavior is better represented by `tap_xy` or `click_locator` actions than by a static locator. Do not overload `new_session_action` with "tap the input box" unless the product truly uses that step to create a new conversation. Runtime only executes `new_session_action` when the sample explicitly sets `new_session=true`; the default sample behavior is to stay in the existing conversation context.
- **Builder new-session authoring:** Builder exposes a guided `tap_xy` sequence flow for `new_session_action`. Select "配置多步新开对话" on the Builder page, choose a step count (1–3), then capture each step to get AI-recommended tap points. Accept or manually override each point; the confirmed sequence is serialized into `new_session_action` in the draft YAML automatically. The flow only outputs `tap_xy` actions. Builder authoring does not execute the new-session flow; runtime still requires `sample.new_session=true`. The step cards distinguish `unavailable` (`当前未配置 VLM，仅支持人工点选`) from `failed` (`推荐请求失败：<reason>`), so users should not need to click "重新点选" just to discover missing VLM configuration.
- **Profile Builder capture/runtime alignment:** `Start Builder Session` enables ADB Keyboard once for the builder session and `Generate Draft` restores the previous IME. `Capture Editing State` is manual-only: the user must focus the input first, and the backend should not auto-tap into editing or run an extra runtime probe during draft generation. Builder now has two draft modes: `rule` keeps manual review mandatory before connectivity validation, while `smart` can auto-apply review decisions and enable connectivity immediately only when the backend returns `requires_manual_review=false`.
- **Profile Builder review completeness:** do not collapse Android builder candidates away before review. For `input_locator`, `input_focus_action`, and `send_action`, keep every bounded node as a review candidate and let recommendation affect ordering only. Keep `latest_bubble_match` review conditional on real ambiguity rather than always forcing the user through it.
- **Visual send controls:** some apps expose the send icon/label as a non-clickable node even though tapping its bounds works. `send_action` candidate generation should keep those visual send controls as tap-only review choices instead of dropping them.
- **Android response extraction:** runtime UI-tree extraction must honor `response_container_locator` before applying `latest_bubble_match`. Do not scan the whole page by class alone when the profile already contains a reviewed response container.
- **Profile Builder response ranking:** keep all `latest_bubble_match` candidates visible in review, but rank likely assistant replies ahead of UI chrome, placeholders, and bottom feature chips so the default recommendation tracks the latest visible assistant message.
- **Response anchors:** treat `response_container_locator` as a structural anchor, not a frozen frame. Runtime extraction should rank matching containers, group multi-`TextView` reply fragments into a single latest response block, prefer the latest valid block inside the best container, and persist `after_result_<n>.xml` plus container-matching logs for device regressions.
- **Final manual smoke doc:** use `docs/superpowers/plans/2026-04-23-plan-4-android-manual-smoke.md` for the final real-device verification run and result-report template.
- **copy_button_vlm blocking-dialog handling:** some apps show a mid-conversation consent/authorization card (e.g. "同意协议") that covers the reply before the copy button is usable. Opt-in per profile via `response_extraction.copy_button_vlm.detect_auth_dialog: true` (off by default — existing profiles' VLM prompt is unchanged unless they opt in). When on, the VLM is asked to report `{"blocking_dialog": true, "dialog_x": .., "dialog_y": ..}` instead of copy-button coords when such a dialog is covering the screen; the executor taps it, waits `dialog_dismiss_wait_sec` (default 2s), and retries locating the copy button on a fresh screenshot — consuming one of the existing VLM attempt retries rather than a separate budget.
- **Tier 2 OCR runtime:** `rapidocr_onnxruntime` runs on CPU in this repo. OCR/stitching paths are marked `@pytest.mark.slow` and stay out of the fast suite.
- **Tier 2 Android smoke:** 1. run a long-response app/profile with `method: ocr_only` or `ui_tree_then_ocr`; 2. verify stitched text spans more than one screen; 3. verify `pixel_stable` or `ui_tree_stable` completes without false positives.
- **Profile Builder artifacts:** stored under `<data_root>/profile_builder/<session_id>/`.
- **Profile Builder rule mode:** draft generation must keep working with no LLM config at all.
- **Profile Builder smart mode:** Builder LLM output is constrained to `draft_overrides` plus `review_decisions`. Review decisions must only pick from surfaced review candidates; if any required field remains unresolved, keep `requires_manual_review=true` and do not pretend the draft is runnable.
- **Profile LLM credentials:** Android profile YAMLs may contain plaintext `base_url`, `model`, and `api_key` for runtime LLM response extraction. Treat `data/profiles/*.yaml` as sensitive local secrets.
- **Runtime LLM creds:** runtime LLM response extraction reads credentials only from the profile YAML. Global `VLMConfig` on the Config page is the default source for Builder draft enrichment and optional YAML injection, not the runtime source of truth.
- **LLM connectivity checks:** `POST /api/v1/config/vlm/test` reports staged connectivity/auth/model/shape failures. `PUT /api/v1/config/vlm` now rejects non-empty invalid triples and only bypasses connectivity checks for a fully empty triple.
- **Profile Builder connectivity:** reuse the existing `/api/v1/tests/sync` execution path (via shared backend helper), not a duplicate executor path.
- **Screenshots:** Web executor screenshots are stored under `<logs_root>/<batch_id>/<sample_id>/NNN_<label>.png`. Milestone screenshots are always captured; intermediate per-action screenshots depend on `verbose_logs`.
- **SSE progress:** `GET /api/v1/batches/{id}/events` is the live progress stream. Frontend `useBatchStream` reconciles updates via `seq`; WebSocket is not used.
- **Self-update:** `src/autoagent/system/updater.py` pulls `origin/main`, runs `uv sync` only when `pyproject.toml`/`uv.lock` changed, `pnpm build`s, then spawns a detached `run.sh restart --no-build` (new session) — all build steps run while the old process still serves, so a broken pull/build aborts with zero downtime. Endpoints `POST /api/v1/system/update/{check,apply}` + `GET /status`; gated behind `DefaultsConfig.self_update_enabled` (off by default, admin-only) — it is RCE-by-design. `apply` returns 409 with `active_batches` when batches are running; UI re-calls with `force=true`. `/health` returns `commit` so the UI can poll for the restart to land. Hourly `git fetch` loop (`system/update_scheduler.py`) keeps `origin/main` fresh so `/status` needs no network; nav shows a dot on Config when behind.
- **STATIC_API_KEY:** optional env var (unset by default). When set, `auth/bearer.py` accepts it as a Bearer token (constant-time compare) on every bearer-protected route — `/api/v1/*` and `/v1/chat/completions` — authenticating as `admin`, alongside normal JWT auth. Lets external scripts/SDKs skip the login step.
- **OpenAI-compat endpoint:** `POST /v1/chat/completions` is a non-streaming shim over the existing sync-test pipeline (`services/sync_tests.py`). `model` → `target_profile`; only the last `user` message is read; `stream=true` is rejected; AutoAgent-specific fields (`new_session`, `timeout_sec`, `retry`, `dry_run`) are passed via the OpenAI SDK's `extra_body` (or top-level JSON keys with raw `curl`). Prefers `llm_responses` over static `responses` when a profile has LLM response extraction enabled.
- **Installer:** `install.sh` (OS detection + system packages: Python 3.11, Node.js, adb, uv, pnpm via Homebrew/apt+deadsnakes/dnf) delegates all Python-level setup to `scripts/setup.py` (`uv sync`, `pnpm build`, `playwright install chromium`, interactive `.env` generation with generated secrets).
- **Batch retention/archiving:** `maintenance/scheduler.py` runs a periodic (~24h) job reading `log_retention_days` from `DefaultsConfig` at each tick (no restart needed to change it). `maintenance/batch_retention.py` deletes terminal (done/failed/cancelled) batches — DB rows + results JSONL + logs dir — older than the retention window; if `archive_retention_days > 0`, each batch is zipped to `data/archive/<batch>.zip` before deletion so an over-aggressive window is recoverable. This is distinct from `scripts/cleanup_runtime_artifacts.py`, the manual CLI tool for the same class of cleanup.
- **Data backup + restore:** `maintenance/backup.py` periodically zips `data/db.sqlite` (via sqlite3's online backup API — WAL-safe) + `data/profiles/*.yaml` to `data/backups/<timestamp>.zip`, controlled by `DefaultsConfig.backup_retention_days`/`backup_interval_hours` (Config page has the fields + a manual "立即备份" trigger + a list of existing backups). There is deliberately no restore UI/endpoint. To restore: stop the service, extract `db.sqlite` from the chosen `data/backups/<timestamp>.zip` over `data/db.sqlite` (and `profiles/*.yaml` over `data/profiles/` if needed), then restart.
- **Device pool:** `devices/pool.py`'s `DevicePool` gives `gui_android`/`agent_android` scheduling a per-serial `asyncio.Lock` checkout (raises `DeviceBusy`/`DeviceDisabled`) so concurrent batches never double-drive the same physical device. Selection is "first free device in `storage/devices.py::list_devices`'s order (online, then most-recently-seen, first)" — not round-robin. With a multi-device profile (`serials`), concurrent samples naturally fan out across devices since each acquire() skips whatever's already locked, but *serial* execution (concurrency < device count) tends to keep reusing the same first-available device rather than rotating.
- **Session-level device stickiness (multi-turn across separate requests):** a conversation can be modeled two ways — multiple `prompts` in one `Sample` (naturally stays on one device, no special handling needed), or a sequence of separate requests chained via `new_session=false` (e.g. sequential `/api/v1/tests/sync` or `/v1/chat/completions` calls). The second pattern needs `Sample.session_id` (opt-in, `None` by default) so all turns land on the *same* physical device: `new_session=true` + `session_id` picks a device normally and pins `session_id` to it; `new_session=false` with a live pin forces that exact device (waits if busy, raises `DeviceDisabled` if it's gone — never silently substitutes a different device, which would silently break the conversation); a missing/expired pin self-heals via normal selection. Reservation is **strict/exclusive**: once a device is pinned to a `session_id`, no other request can pick it up — another session's `new_session=true`, *and* a plain request with no `session_id` at all — until it's released or expires. `session_id` omitted only reproduces the pre-feature behavior when nothing else in the system currently holds a session pin — "reserved but not locked" is a state that didn't exist before this feature, so there's no prior behavior to preserve once something is actually reserved; in the total absence of any session traffic, behavior is unchanged. Pins are in-memory on `DevicePool` (bounded to 10k, oldest-evicted, same pattern as `auth/login_throttle.py`), with a 30min inactivity TTL as a fallback for callers that never explicitly end the conversation.
  Two different outcomes when every device is currently reserved: a plain (no `session_id`) request still *waits* — same poll/timeout as a locked device — since it has no session context to act on. A `session_id`-bearing call instead raises `DeviceReserved` (a `DeviceBusy` subclass) *immediately*, naming which `session_id`(s) hold every device (`e.blocking_session_ids`). `scheduler/batch_scheduler.py::run_one` catches this (along with plain `DeviceBusy`/`DeviceDisabled` — previously **uncaught**, silently crashing the batch's background task before it ever reached `update_batch_status`, leaving the batch stuck showing "running" forever) and records a normal failed `SampleResult`, with `metadata["blocking_session_ids"]` set for the `DeviceReserved` case. `services/sync_tests.py::blocking_session_ids()` reads that back out; both `/api/v1/tests/sync` and `/v1/chat/completions` use it to turn that specific failure into an actual HTTP `429` (`detail`/`error` body carries the blocking session ids) instead of a normal `200` with `status: "failed"` embedded — every other failure reason still returns 200/`status: "failed"` (or the OpenAI-compat equivalent) as before.
  `Sample.end_session=true` ends a conversation without sending a real turn: skips profile lookup, device acquisition, and execution entirely, just calls `DevicePool.release_session(session_id)` and returns `status="done"` immediately (`prompts` is still required by the schema but ignored). Freeing the reservation immediately like this — instead of waiting out the 30min TTL — is what makes multi-conversation device pools actually usable; without an explicit end signal there'd be no way to reclaim a device short of waiting it out. Wired through `openai_compat/schemas.py`'s `end_session` too.
  Call `POST /api/v1/devices/sessions/{session_id}/release` for the same immediate-release effect without sending a `Sample` at all (e.g. a caller that isn't mid-conversation-turn-shaped) — safe to call unconditionally (`released: false` if there was nothing to free, not an error).
- **Notifications:** `notifications/dingtalk.py` sends DingTalk custom-robot webhook alerts. Two triggers: device online→offline/missing transitions (and back), and an `empty_response_streak` rule — N consecutive same-response samples from one device fire an alert (VLM-judged via `vlm_judge.py` to rule out a legitimately static reply), suppressible per-profile via a persisted whitelist (`whitelist.py`).
- **"Effective response" semantics:** `select_effective_response()` (`openai_compat/chat_completions.py`) is the single definition of "what response did this sample actually produce" — it prefers `llm_responses[0]` over raw `responses` when LLM review ran, produced non-empty text, and reported no error; otherwise falls back to raw. Any code that displays or filters on "the response" (batch list preview, `empty_response_only` filter in `storage/batches.py::_is_empty_response_clause`, `/v1/chat/completions`, both DingTalk notification rules in `notifications/rules.py`) must mirror this exact condition or it'll drift out of sync with what the API actually returns — this happened three times now (2026-07-14 fixes to batch list preview + `empty_response_only`; 2026-07-27 fix to the DingTalk rules, which had never been aligned at all and were false-alerting on profiles with LLM extraction enabled) before each call site was aligned with the SQL-level `_has_effective_llm_response_clause()` or the `select_effective_response()` helper directly.

## Common commands

```bash
bash install.sh                        # one-shot install: system deps + uv sync + pnpm build + playwright + .env
uv run pytest -q                       # run all tests
uv run pytest -q -m "not playwright"   # skip real-browser tests
uv run pytest -q -m "not playwright and not android and not slow"   # fast backend suite
uv run pytest -v -m android            # android real-device suite
uv run pytest tests/unit -v            # unit only
uv run ruff check .                    # lint
uv run ruff format .                   # format
uv run alembic revision --autogenerate -m "describe the change"   # new migration after editing models/db.py
uv run alembic upgrade head            # apply migrations to the dev DB directly (init_db() also does this at boot)
uv run python3 scripts/cleanup_runtime_artifacts.py --days 7          # preview old logs/profile-builder artifacts
uv run python3 scripts/cleanup_runtime_artifacts.py --days 7 --apply  # delete old logs/profile-builder artifacts
uv run python3 scripts/cleanup_runtime_artifacts.py --all --apply     # clear all logs/profile-builder artifacts
uv run playwright install chromium     # one-time: download Chromium
adb devices -l                         # verify adb sees local devices
uv run uvicorn --app-dir src autoagent.main:app --reload   # run dev server
cd web && pnpm dev                     # frontend dev server (5173)
cd web && pnpm build                   # build UI into src/autoagent/static/
cd web && pnpm test                    # frontend unit tests
cd web && pnpm lint                    # frontend lint
```

Environment for running:
- **Defaults** (dev): `ADMIN_USERNAME=admin`, `ADMIN_PASSWORD=admin123456`, `JWT_SECRET=dev-secret-key-32-chars-minimum-length`. These are built into `src/autoagent/config/settings.py` for convenience; override via `.env` or env vars in production.
- **Optional**: `CORS_ORIGINS` (comma-separated). Default empty → `CORSMiddleware` not mounted (SPA ships same-origin). Set only for cross-origin dev setups, e.g. `CORS_ORIGINS=http://localhost:5173`.
- **Optional**: `STATIC_API_KEY` — unset by default; when set, provides a long-lived Bearer key alongside JWT (see Conventions).

ADB Keyboard APK is bundled at `src/autoagent/fixtures/ADBKeyboard.apk` (17 KB). The `/devices` page has one-click "Install ADB Keyboard" and toggle "Enable/Disable IME" buttons.

## When starting a new task in this repo

1. Check the plan file for the plan currently in progress (see "Development status").
2. Consult `docs/superpowers/specs/2026-04-21-agent-ai-testing-tool-design.md` for architecture intent.
3. Match existing code style — most modules are small and single-purpose; prefer adding a new module over growing an existing one past ~200 lines.
4. Plan 5 work should start with the "secrets + auth hardening" task (see "Deferred work").
5. If working on Android/Profile Builder, read `docs/superpowers/plans/2026-04-24-android-profile-builder-handoff.md` before debugging or retesting.
6. If working on LLM extraction behavior, also read `docs/superpowers/specs/2026-04-25-llm-response-extraction-design.md`.
7. If working on the `agent_pc`/`agent_android` runtime, read `docs/superpowers/plans/2026-05-06-agent-runtime-unification.md` for the current coordinate contract and observer-guardrail behavior before changing `executors/agent_core/`.
8. Plans 1-4 and 6 are complete and tagged; Plan 5 (packaging/security hardening) has not started. Everything since Plan 6 (agent runtime unification, install script, OpenAI compat, static API key, device streaming, and small batches/`copy_button_vlm` fixes) has landed directly on `main` without its own numbered plan — check `git log` for anything more recent than this file.
